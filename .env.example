# =============================================================================
# Homelab Control Center — Environment configuration
# =============================================================================
# Copy this file to .env and fill in your values. Never commit .env to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Required (unless DEMO_MODE=true)
# -----------------------------------------------------------------------------

# Portainer CE base URL (no trailing slash)
PORTAINER_URL=https://portainer.example.com

# Portainer endpoint ID (Docker environment). Get it via: GET /api/endpoints with your JWT
PORTAINER_ENDPOINT_ID=1

# -----------------------------------------------------------------------------
# Authentication — use one of the two methods below
# -----------------------------------------------------------------------------

# Option A: API key (recommended for automation). Create in Portainer: Users → your user → API keys
# If set, PORTAINER_USERNAME and PORTAINER_PASSWORD are ignored.
PORTAINER_API_KEY=

# Option B: Username and password (used to obtain a JWT at runtime)
PORTAINER_USERNAME=admin
PORTAINER_PASSWORD=

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------

# Set to "true" to allow self-signed or invalid TLS certificates when connecting to Portainer.
# Use only in trusted environments (security risk).
PORTAINER_INSECURE_TLS=false

# Set to "true" to run without Portainer: mock containers, fake logs, no real API calls.
# Useful for local testing or screenshots. No secrets required.
DEMO_MODE=false

# -----------------------------------------------------------------------------
# Minecraft status (server list ping) — optional, internal API only
# -----------------------------------------------------------------------------
# MINECRAFT_HOST=192.168.1.20
# MINECRAFT_PORT=25565
# MINECRAFT_TIMEOUT_MS=1500
